import React from 'react';
import Card from './DraggingCard';


const RankedQuestion = (props) => (
	<div className="container-drag">
	<p>{props.ExampleTag}</p>
	<p>{Card}</p>
        DRAG & DROP DEMO
      </div>
	);

export default RankedQuestion